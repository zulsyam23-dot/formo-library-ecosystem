# Docs

Dokumentasi library language-style.

Ringkasan cepat:

- Parser `.fs` memvalidasi token + allowlist style.
- Output style disimpan sebagai `decls` dan `canonicalDecls`.
- Canonical style dipakai oleh backend web/desktop lewat `effective_style_decls(...)`.
